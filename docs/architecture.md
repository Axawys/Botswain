# Архитектура Botswain

Документ фиксирует ключевые решения и потоки. Детали API — в
[control-api.md](control-api.md).

## Общая картина

Desktop-приложение (Flutter, Linux) управляет Telegram-ботами на удалённом VPS
и на локальном ПК. Пользователь вводит ip/login/password, приложение само по SSH
ставит Docker и поднимает агента, дальше человек из GUI заводит «профиль бота»,
выбирает python-файл, и бот запускается на сервере как контейнер с
автоперезапуском.

Метрики, egress-прокси, Android-клиент и биометрия — за пределами текущих
milestone'ов, см. раздел «Milestone'ы».

## Две вершины монорепо

- `/agent` — Go. Один статический бинарник, живёт **в контейнере**, монтирует
  `/var/run/docker.sock` и управляет ботами как соседними контейнерами
  (docker-out-of-docker). Раздаёт control-API, биндится на loopback.
- `/app` — Flutter desktop (Linux). Вся не-UI логика вынесена в отдельный Dart-
  пакет `app/packages/botswain_core` (SSH, туннель, API-клиент, bootstrap,
  секреты, модели); UI — тонкий слой поверх. Причина: позже переиспользуем ядро
  в Android-клиенте без переписывания.
- `/docs` — общий контракт, виден обеим вершинам.

## Потоки

### Bootstrap сервера (только SSH)

```
app ──SSH exec──► VPS: docker --version        (проверка)
app ──SSH exec──► VPS: docker run … botswain-agent
app ──SSH port-forward──► 127.0.0.1:PORT (агент на VPS)
```

SSH используется **только** для bootstrap и для поднятия туннеля. Произвольные
команды из GUI по SSH не гоняются.

### Работа с агентом (только control-API)

```
app ─HTTP/WS через SSH-туннель─► agent (127.0.0.1:PORT в контейнере)
                                   └─ docker.sock ─► боты-контейнеры
```

Публично агент в сеть не торчит — туннель это и есть аутентификация и шифрование.

### Локальный режим

Тот же агент на `127.0.0.1` напрямую, тот же API, без туннеля. Docker требуется
и локально: паритет прод/тест важнее zero-setup.

## Ключевые решения

- **Клиент = инсталлятор.** Отдельного инсталлятора нет: клиент по SSH
  проверяет/ставит Docker, делает `docker run` агента, дожидается health-check.
- **Роутер агента — `chi`.** Тонкая обёртка над `net/http` (хендлеры остаются
  `http.HandlerFunc`, без вендор-лока), даёт группировку `/v0`, middleware
  (request-id, recover) и готовность к WebSocket-логам в M3. `echo`/`gin`
  избыточны; чистый `net/http` пришлось бы обвязывать вручную уже в M1.
- **Боты — соседние контейнеры** с `--restart unless-stopped` и лимитами
  ресурсов (`--memory`, `--cpus`). (M2.)
- **Секреты** (SSH-пароль/ключ, токены ботов) — только через
  `flutter_secure_storage` (Linux → libsecret). Никогда в конфиге открытым
  текстом.
- **Устойчивость туннеля.** Сетевой слой клиента переживает разрыв туннеля:
  переподключение с экспоненциальным backoff. Заложено с самого начала.

## Milestone'ы

- **M1 (текущий):** сквозной путь «подключились → агент работает → отвечает».
  Health-эндпоинт, bootstrap, туннель, reconnect. Ботов/метрик/прокси нет.
- **M2:** жизненный цикл ботов (профиль, загрузка python, запуск контейнера).
- **M3:** метрики CPU/RAM, стриминг логов (WebSocket).
- **M4:** egress-прокси к `api.telegram.org` (счётчик трафика + обход блокировок).
- **Позже:** Android-клиент, биометрия, упаковка (AppImage/Flatpak/.deb).
